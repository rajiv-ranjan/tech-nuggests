[project]
name = "users-groups-rbac"
version = "0.2.1"
description = "OpenShift User Management with htpasswd authentication"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "python-dotenv>=1.0.0",
]

[project.scripts]
oc-user-manager = "main:main"
